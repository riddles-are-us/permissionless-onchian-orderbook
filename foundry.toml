[profile.default]
src = "."
out = "out"
libs = ["lib"]
test = "test"
cache_path = "cache_forge"

# Solc 版本
solc = "0.8.20"

# 优化
optimizer = true
optimizer_runs = 200

# EVM 版本
evm_version = "paris"

# 详细输出
verbosity = 3

# Gas 报告
gas_reports = ["*"]
