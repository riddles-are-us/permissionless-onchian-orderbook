{
  "name": "orderbook-test",
  "version": "1.0.0",
  "description": "OrderBook system with Hardhat",
  "scripts": {
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "update-config": "node update_config.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}
